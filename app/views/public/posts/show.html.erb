<h1>投稿詳細</h1>

<h2><%= @post.title %></h2>

<p><%= @post.body %></p>

<% if @post.images.attached? %>
  <% @post.images.each do |image| %>
    <%= image_tag image, size:'100x100' %>
  <% end %>
<% end %>
