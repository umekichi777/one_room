<h1>グループ詳細</h1>


<div>
  <%= image_tag @group.get_group_image(100,100) %>
</div>

<h2>
  <%= @group.name %>
</h2>
<p>
  owner: <%= @group.user ? @group.user.user_name : 'unknown' %>
  <i class="far fa-clock"></i><%= @group.created_at.strftime('%Y/%m/%d') %> 作成
</p>

<h4>グループ概要</h4>
<p><%= @group.introduction %></p>

<div>
  <% @group.users.each do |member| %>
    <%= member.user_name %>
  <% end %>
</div>